server:
  port: 9004

spring:
  application:
    name: provider-test
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: "*"