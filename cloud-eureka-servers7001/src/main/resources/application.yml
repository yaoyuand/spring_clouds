server:
  port: 7001

eureka:
  instance:
    hostname: localhost  #服务端实列名称
  client:
    register-with-eureka: false  #不像注册中心注册自己
    fetch-registry: false        #表示自己是注册中心，只负责维护服务，不检索服务
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/   #服务访问地址

